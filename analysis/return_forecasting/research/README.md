# Return Forecasting Research Notes

Each model module has a short research note in this directory. The benchmark runner
copies these notes into each run output, so model comparisons remain tied to the
method assumptions used for that run.

